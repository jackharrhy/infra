{
  "name": "jack-infra",
  "main": "index.ts",
  "scripts": {
    "fmt": "oxfmt",
    "lint": "oxlint --fix",
    "pulumi:preview": "set -a; . ./.env; set +a; PULUMI_BACKEND_URL=\"file://$PWD\" \"$HOME/.pulumi/bin/pulumi\" preview --stack dev",
    "pulumi:up": "set -a; . ./.env; set +a; PULUMI_BACKEND_URL=\"file://$PWD\" \"$HOME/.pulumi/bin/pulumi\" up --stack dev"
  },
  "dependencies": {
    "@pulumi/aws": "^7.0.0",
    "@pulumi/awsx": "^3.0.0",
    "@pulumi/pulumi": "^3.113.0"
  },
  "devDependencies": {
    "@types/node": "^18",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.51.0",
    "typescript": "^5.0.0"
  }
}
