[Unit]
Description=Refresh the CADO registry snapshot
Wants=network-online.target
After=network-online.target docker.service
Requires=docker.service

[Service]
Type=oneshot
WorkingDirectory=/home/jack/infra/hosts/newport
ExecStart=/usr/bin/docker compose --profile tools run --rm cado-refresh
TimeoutStartSec=infinity
